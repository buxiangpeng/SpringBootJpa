server:
  port: 8888
  # tomcat设置
  tomcat:
    accesslog:
      #开启日志访问
      enabled: true
      #日志保存路径
      directory: E:/
      
  
  
logging:
  level:
    com:
      example:
        dao: debug
        
spring: 
    datasource: 
      url: jdbc:mysql://localhost:3306/my_test?useSSL=false
      username: root
      password: ds
      driver-class-name: com.mysql.jdbc.Driver
    jpa: 
      hibernate:
        ddl-auto: update
      show-sql: true
